maintainer = "Fnkoc"
pkgname = "wpscan"
versao = 2.60
process = {
	"cd .cache/wpscan",
	"bundle install --without test --path vendor",
	"bundle update",
	"cd .."
	}

installer = True
type = ruby
installer_type = script
